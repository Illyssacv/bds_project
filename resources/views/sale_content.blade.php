@extends('app')
@section('content')
    <div class="container mt-4">
        <h2 class="mb-4">Nhà Đất Bán</h2>
        <div class="row">
            <!-- @foreach($salePosts as $post) -->
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <!-- <img src="{{ $post->image_url }}" class="card-img-top" alt="..."> -->
                        <div class="card-body">
                            <h5 class="card-title